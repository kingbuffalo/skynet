local M = { }

--function M.init()
	--local item = require("game.config.item")
	--local npc = require("game.config.npc")
	--local map = require("game.config.map")
	--local hero = require("game.config.hero")
	--local datacenter = require("datacenter")
	--datacenter.set("item",item)
	--datacenter.set("npc",npc)
	--datacenter.set("map",map)
	--datacenter.set("hero",hero)
--end

--function M.getConfig(name)
	--local datacenter = require("datacenter")
	--return datacenter.get(name)
--end

--function M.init()
	--local sharedata = require ("skynet.sharedata")
	--local item = require("game.config.item")
	--local npc = require("game.config.npc")
	--local map = require("game.config.map")
	--local hero = require("game.config.hero")
	--sharedata.new("item",item)
	--sharedata.new("npc",npc)
	--sharedata.new("map",map)
	--sharedata.new("hero",hero)
--end

--function M.getConfig(name)
	--local sharedata = require ("skynet.sharedata")
	--return sharedata.query(name)
--end


return M
