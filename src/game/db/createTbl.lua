local M = {}

--local ENEMY_START_ID = 10000000
--function M.createArmyByCfg(npcCfg)
	--return { armyId = ENEMY_START_ID + npcCfg.hero_id,
		--heroId = npcCfg.hero_id,
		--count = npcCfg.army_count,
		--x = npcCfg.pos[1],
		--y = npcCfg.pos[2], }
--end
--function M.createArmyByHeroTbl(heroTbl,armyCount,x,y)
	--return { armyId = heroTbl.hid,
		--heroId = heroTbl.hid,
		--count = armyCount,
		--x = x, y = y,}
--end

return M
